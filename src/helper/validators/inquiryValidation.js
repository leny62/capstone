// const Joi = require('joi')

// exports.validateInquiry = (body) => {
//     const schema = {
//         name: Joi.string().min(2).max(30).required(),
//         email: Joi.string().min(2).max(100).required(),
//         message: Joi.string().min(2).required()
//  }
//  return Joi.validate(body,schema)
// }